export enum RoleEnum{
    admin = 'Admin',
    user = 'User',
    seller = 'Seller'
}

export enum ModelStatus{
    active = 'Active',
    inactive = 'Inactive',
    deleted = 'Deleted'
}

export enum Status{
    available = 'Available',
    out_of_stock = 'Out_of_stock'
}